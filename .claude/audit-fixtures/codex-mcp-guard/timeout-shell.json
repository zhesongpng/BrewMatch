{
  "_doc": "Timeout payload — covers cc-artifacts.md Rule 7. Server must allow the call (fail-open) AND log to violations.jsonl when a hook subprocess exceeds SUBPROCESS_TIMEOUT_MS. Exercised via test-server.mjs against a synthetic sleeping hook because no production hook deliberately hangs.",
  "tool": "shell",
  "tool_input": { "command": "ls" },
  "expected_allow": true,
  "expected_violation_kind": "codex_mcp_guard_timeout"
}
