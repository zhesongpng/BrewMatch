# BrewMatch Environment Configuration
# Copy to .env: cp .env.example .env

# LLM Configuration (for bean profile extraction)
LLM_MODEL=your-model-name-here
LLM_API_KEY=your-api-key-here

# Demo mode (uses in-memory SQLite instead of file-based)
BREWMATCH_DEMO_MODE=false
