{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Development MCP configuration for Kailash projects. Context cost: ~17k tokens.",
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "description": "GitHub operations: PRs, issues, actions"
    },
    "memory": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Session persistence across restarts"
    },
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "--root", "."],
      "description": "File system operations in project root"
    }
  }
}
