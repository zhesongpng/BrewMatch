# Red Team — B1.1 Implementation (Bag data model)

Date: 2026-06-11 · Posture: **L5_DELEGATED** (Round 1 optional; run at user request)
Scope: `/redteam B1.1 implementation first` — code-level audit of the bag data
model. Full assertion table: `workspaces/BrewMatch/.spec-coverage-v2.md`.

## What was verified

- **Spec compliance (AST):** every field of `BeanProfile` (roaster/name) and
  `CoffeeBag` matches `specs/data-models.md` §2 / §2.5 exactly — verified by
  parsing the source AST, not file existence. 100% green.
- **ML-safety (the big risk):** adding two fields to `BeanProfile` could have
  polluted the taste model's feature vector. Cleared — `taste_predictor/
encoder.py` builds features from explicitly named fields only; no `asdict`,
  no field enumeration, no positional construction anywhere in `src/`.
- **Test run:** 663 unit tests pass (+2 this round); `--collect-only` clean;
  `-W error` shows zero warnings (log-triage gate clean).

## Findings

- **M1 (MED) — backward-compat test covered the wrong function. FIXED.**
  The B1.1 claim "old brew records still deserialize" is about the persistence
  path (`db.py:_deserialize_bean`, `BeanProfile(**d)`). The original test
  exercised `utils.dict_to_bean_profile` — a different function with explicit
  `.get()` per field. The actual persistence path was unproven by any test.
  Fixed this round: added `test_legacy_bean_json_without_roaster_name_
deserializes` (`@pytest.mark.regression`) + `test_brew_roundtrip_preserves_
roaster_and_name` to `tests/unit/test_db.py`, both exercising the real db
  serialize/deserialize path. Verified the path works for old (keys absent →
  None) and new (round-trip preserves values) records.

No CRITICAL, no HIGH. One MED, fixed in-round.

## Convergence

| Criterion                     | Status                                |
| ----------------------------- | ------------------------------------- |
| 0 CRITICAL                    | ✅                                    |
| 0 HIGH                        | ✅                                    |
| Spec 100% AST-verified        | ✅                                    |
| New code has new tests        | ✅ (CoffeeBag + roaster/name covered) |
| Frontend mock data            | N/A (no frontend in B1.1)             |
| Clean re-derivation after fix | ✅ (663 pass, 0 warnings)             |

**Disposition: B1.1 converged.** Ready to proceed to B1.2 on user approval.
